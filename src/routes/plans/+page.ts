// No dynamic data on this page, so it prerenders to a static asset. It
// ships the client too, since the redesign: the questions open, the bars grow
// and the header steps back while the page's own yellow is on screen.
export const prerender = true;
